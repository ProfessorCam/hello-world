#include <stdio.h>
int main() {
   // printf() displays the string inside quotation
   printf("Hello, World in C from Github!");
   return 0;
}

//To run this command in terminal you will need to run the following command:
//gcc hello-world.c -o hello-world
